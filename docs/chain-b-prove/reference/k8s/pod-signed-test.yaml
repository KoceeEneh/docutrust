# Reference copy. Project 6 admission test pod (signed image).
# Replace the digest with the one from your own build.
apiVersion: v1
kind: Pod
metadata:
  name: docutrust-signed-test
spec:
  containers:
    - name: docutrust
      image: ghcr.io/<owner-lowercase>/docutrust@sha256:<signed-image-digest>
